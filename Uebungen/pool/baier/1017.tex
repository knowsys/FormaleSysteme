\begin{exercise}
Sei $\T_w$ wie in Aufgabe~1 und 
\[
  t_{\cT_w} (x)
  \ \eqdef\
  \mbox{Anzahl der Schritte, die $\cT_w$ bei
        Eingabe $x$ durchf\"uhrt.}
\]

Ist die Sprache $L = \{ w \in \{0,1\}^* \, \mid t_{\T_w}(w) > 2^{|w|} \}$ entscheidbar?
Begr\"unden Sie Ihre Antwort.
%\\[1ex]
%Wie in der Vorlesung bezeichnet 
% $\T_w$ eine
%deterministische Einband-Turingmaschine mit dem
%    Eingabealphabet $\{0,1\}$, deren Codewort gleich $w$ ist,
%    falls es eine solche gibt.
%    Andernfalls ist $\T_w$ eine feste nichtdeterministische 
%    Turingmaschine, die f\"ur jede Eingabe endlos l\"auft.
%$t_{\cT_w} \colon \{0,1\}^* \to \Nat \cup \{\infty\}$ bezeichnet die 
%zugeh\"orige Kostenfunktion f\"ur die Zeitkomplexit\"at, also
%$$ 
%  t_{\cT_w} (x)
%  \ \eqdef\
%  \mbox{Anzahl der Konfigurationswechsel, die $\cT_w$ bei
%        Eingabe $x$ durchf\"uhrt.}
%$$
\end{exercise}
